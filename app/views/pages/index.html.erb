<h1>Configuration</h1>
<p>You must configure at least one of each of the categories below.</p>
<table class="table table-striped table-responsive table-hover">
  <thead>
    <th>Client</th>
    <th>Product</th>
    <th>Status</th>
    <th>Priority</th>
  </thead>
  <tbody>
    <tr>
      <td><%= link_to "Show All Clients", clients_path, class: "btn btn-info" %></td>
      <td><%= link_to "Show All Products", products_path, class: "btn btn-info" %></td>
      <td><%= link_to "Show All Statuses", statuses_path, class: "btn btn-info" %></td>
      <td><%= link_to "Show All Priorites", priorities_path, class: "btn btn-info" %></td>
    </tr>
    <tr>
      <td><%= link_to "Add New Client", new_client_path, class: "btn btn-success" if can? :create, Client %></td>
      <td><%= link_to "Add New Product", new_product_path, class: "btn btn-success" if can? :create, Product%></td>
      <td><%= link_to "Add New Status", new_status_path, class: "btn btn-success" if can? :create, Status%></td>
      <td><%= link_to "Add New Priority", new_priority_path, class: "btn btn-success" if can? :create, Priority %></td>
    </tr>
  </tbody>
</table>