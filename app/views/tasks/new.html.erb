<% if Client.count == 0 || Product.count == 0 || Status.count == 0 || Priority.count == 0 %>
  <h2 style="color: red;">Woops!  It looks like you have not performed the proper <%= link_to "configuration", pages_index_path %> yet.  Please set this up before continuing!</h2>
<% end %>
<%= render 'errors' %>
<div class="dark-matter">
  <h1>New Task 
      <span>Start by entering some basic information about the task</span>
  </h1>
  <%= render 'form' %>
</div>
<%= link_to "All Tasks", tasks_path %>
