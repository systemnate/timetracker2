<%= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>
  <div class="form-group">
    <%= f.text_field :content, placeholder: "New Todo Item", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit "Add", class: "btn btn-success form-control" %>
  </div>
<% end %>
